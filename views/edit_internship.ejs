<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Internship</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="header">
        <div class="title">
            <h1>Internship Management</h1>
        </div>
        <div class="nav-div">
            <nav>
                <a href="/admin/internship">Dashboard</a>
                <a href="/admin/profile">Profile</a>
                <a href="/">Home</a>
            </nav>
        </div>
    </div>

    <div class="form-container">
        <h2>Edit Existing Internship</h2>

        <form method="post">
            <div class="form-group">
                <label for="title">Internship Title</label>
                <input type="text" id="title" name="title" placeholder="e.g. Frontend Developer Intern" value="<%=title%>" required />
            </div>

            <div class="form-group">
                <label for="location">Location</label>
                <input type="text" id="location" name="location" placeholder="e.g. Remote, Dubai Marina, etc." value="<%=location%>" required />
            </div>

            <div class="form-group">
                <label for="type">Type</label>
                <select id="type" name="type" required>
                    <option value="Remote" <% if (location == "Remote") {%>selected<%}%>>Remote</option>
                    <option value="On-site" <% if (location == "On-site") {%>selected<%}%>>On-site</option>
                </select>
            </div>

            <div class="form-group">
                <label for="skills">Required Skills</label>
                <input type="text" id="skills" name="skills" placeholder="e.g. JavaScript, HTML, CSS, React" value="<%=skills%>" required/>
            </div>

            <div class="form-group">
                <label for="salary">Salary</label>
                <input type="text" id="salary" name="salary" placeholder="0.00" pattern="^(0\.\d{1,2}|[1-9]{1}\d*(\.\d{1,2})?)$" value="<%=salary%>" required />
            </div>

            <div class="form-group">
                <label for="duration">Duration (in months)</label>
                <input type="number" id="duration" name="duration" min="1" max="12" value="<%=duration%>" required />
            </div>

            <div class="form-group">
                <label for="deadline">Application Deadline</label>
                <input type="date" id="deadline" name="deadline" value="<%=deadline%>" required />
            </div>

            <div class="form-group">
                <label for="description">Internship Description</label>
                <textarea id="description" name="description" rows="6" placeholder="Describe the internship responsibilities, expectations, etc."><%=description%></textarea>
            </div>

            <button type="submit" class="submit-button">Save Changes</button>
        </form>
    </div>
</body>
</html>
